# 📱 توجيهات محادثة اللوحة (Dashboard)
## مع دروس آباء التصميم العشرة
### للنسخ واللصق مباشرة

---

```
═══════════════════════════════════════════════════════════════
     🎯 مهمة التكامل مع Python Backend + تحسين UX
═══════════════════════════════════════════════════════════════

مرحباً! لدينا مهمتان:
1. ربط Dashboard مع Python Backend
2. تطبيق دروس آباء التصميم لتحسين التجربة

البيانات: diwan_iqraa_elmi (157 مليون صف)
Backend: port 8000

═══════════════════════════════════════════════════════════════
                الجزء 1: ربط الـ Backend
═══════════════════════════════════════════════════════════════

1️⃣ أنشئ: dashboard/src/lib/api/backend-client.ts

const BACKEND_URL = process.env.NEXT_PUBLIC_BACKEND_URL || 'http://localhost:8000';

export interface SearchRequest {
  query: string;
  corpus?: 'fiqh' | 'hadith' | 'kalam' | 'usul' | 'timeline' | 'all';
  limit?: number;
}

export interface SearchResult {
  id: string;
  content: string;
  title?: string;
  author?: string;
  corpus: string;
}

class BackendClient {
  private baseUrl: string;

  constructor(baseUrl: string = BACKEND_URL) {
    this.baseUrl = baseUrl;
  }

  async search(request: SearchRequest): Promise<SearchResult[]> {
    const response = await fetch(`${this.baseUrl}/api/search`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(request),
    });
    if (!response.ok) throw new Error('Search failed');
    const data = await response.json();
    return data.results;
  }

  async getStats(): Promise<any> {
    const response = await fetch(`${this.baseUrl}/api/stats`);
    return response.json();
  }

  async runPipeline(query: string, mode: string = 'full'): Promise<any> {
    const response = await fetch(`${this.baseUrl}/api/pipeline`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ query, mode }),
    });
    return response.json();
  }

  async healthCheck(): Promise<boolean> {
    try {
      const response = await fetch(`${this.baseUrl}/health`);
      return response.ok;
    } catch {
      return false;
    }
  }
}

export const backendClient = new BackendClient();

───────────────────────────────────────────────────────────────

2️⃣ أنشئ: dashboard/.env.local

NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

───────────────────────────────────────────────────────────────

3️⃣ حدّث Orchestrator لاستخدام Backend:

أضف في الأعلى:
import { backendClient } from '@/lib/api/backend-client';

ثم استبدل mock بـ:
const results = await backendClient.search({
  query: userQuery,
  corpus: 'all',
  limit: 20
});

═══════════════════════════════════════════════════════════════
     الجزء 2: دروس آباء التصميم (الأهم لـ IQRA-12)
═══════════════════════════════════════════════════════════════

من Jon Friedman (Microsoft):
─────────────────────────────────────────
• أظهر "ما الذي يحدث الآن" (Status + Progress)
  → أضف شريط تحميل واضح أثناء البحث في 157M صف
  
• صمّم لحالات الشبكة: Offline / Slow / Partial failure
  → أضف رسائل خطأ ودية + زر إعادة المحاولة

• "القابلية للتفسير" أهم من "الدهشة"
  → اشرح لماذا ظهرت هذه النتيجة (corpus + relevance)

───────────────────────────────────────────────────────────────

من Steve Kaneko (Windows):
─────────────────────────────────────────
• التحميل التدريجي (Skeleton/Incremental rendering)
  → اعرض النتائج كلما وصلت، لا تنتظر الكل

• "بحث داخل اللوحة" كمسار أساسي
  → البحث هو قلب IQRA-12، اجعله بارزاً ومركزياً

• احترم الأداء: لوحة بطيئة = تجربة فاشلة
  → Cache النتائج + Debounce للاستعلامات

───────────────────────────────────────────────────────────────

من Jensen Harris (Office Ribbon):
─────────────────────────────────────────
• "أفضلية المسار السريع": الأكثر شيوعًا على بعد نقرة واحدة
  → البحث + آخر الاستعلامات + المفضلات في الواجهة الرئيسية

• طبّق "الإظهار حسب السياق"
  → إذا اختار المستخدم corpus معين، أظهر فلاتر مناسبة له

• صمّم حول "ما ينجزه المستخدم" لا "ما يستطيع النظام فعله"
  → المستخدم يريد "إجابة"، لا "قائمة نتائج"

───────────────────────────────────────────────────────────────

من Raymond Chen (Windows):
─────────────────────────────────────────
• صمّم بافتراض أن المستخدم لا يقرأ
  → استخدم أيقونات واضحة + ألوان دلالية للـ corpus

• الافتراضي يجب أن يكون آمنًا
  → ابدأ بـ corpus: 'all' و limit: 20

• اجعل الإلغاء (Cancel) متاحًا أثناء العمليات الطويلة
  → زر إلغاء للبحث الطويل

───────────────────────────────────────────────────────────────

من Don Norman (Apple):
─────────────────────────────────────────
• تغذية راجعة فورية (Feedback)
  → عند الكتابة: "جاري البحث في 157 مليون نص..."

• قلل الحمل على الذاكرة
  → احفظ سياق البحث في URL (يمكن مشاركته/العودة إليه)

• "المعلومات وقت الحاجة" (Just-in-time help)
  → تلميحات صغيرة: "جرب: حكم الزكاة في المذهب الشافعي"

───────────────────────────────────────────────────────────────

من Susan Kare (Apple Icons):
─────────────────────────────────────────
• أيقونات واضحة للـ 5 corpora:
  → 📖 fiqh (الفقه)
  → 📜 hadith (الحديث)
  → 🕐 timeline (التاريخ)
  → 💭 kalam (الكلام)
  → ⚖️ usul (الأصول)

• لا تعتمد على اللون وحده (عمى الألوان)
  → أيقونة + لون + نص

───────────────────────────────────────────────────────────────

من Imran Chaudhri (Humane):
─────────────────────────────────────────
• قلّل عدد الخطوات: تفاعل واحد قد يحل 3 مشاكل
  → اكتب واضغط Enter = بحث فوري

• اجعل الواجهة "تظهر وتختفي" حسب الحاجة
  → الفلاتر المتقدمة تظهر عند الحاجة فقط

───────────────────────────────────────────────────────────────

من Yang Zhiyan (Huawei/HarmonyOS):
─────────────────────────────────────────
• وفّر وضع "السيناريوهات" (Scenes)
  → "باحث": نتائج مفصلة + مصادر كاملة
  → "طالب": ملخص + شرح مبسط
  → "مفتي": أدلة + أقوال العلماء

───────────────────────────────────────────────────────────────

من Abigail Brody (Huawei):
─────────────────────────────────────────
• Analytics مخصص للوحة التحكم
  → سجل: (بحث، نقر على نتيجة، وقت القراءة، مشاركة)

• "Recommended defaults" بدل ترك المستخدم في متاهة
  → اقتراحات: "ابحث في الفقه أولاً" للأسئلة الفقهية

═══════════════════════════════════════════════════════════════
              الجزء 3: مكونات مقترحة للتنفيذ
═══════════════════════════════════════════════════════════════

4️⃣ أنشئ مكون حالة البحث (تطبيق دروس Friedman + Norman):

📁 dashboard/src/components/search/search-status.tsx

interface SearchStatusProps {
  status: 'idle' | 'loading' | 'success' | 'error';
  resultCount?: number;
  latencyMs?: number;
  error?: string;
}

export function SearchStatus({ status, resultCount, latencyMs, error }: SearchStatusProps) {
  if (status === 'idle') return null;
  
  if (status === 'loading') {
    return (
      <div className="flex items-center gap-2 text-blue-600">
        <Spinner />
        <span>جاري البحث في 157 مليون نص...</span>
      </div>
    );
  }
  
  if (status === 'error') {
    return (
      <div className="flex items-center gap-2 text-red-600">
        <AlertIcon />
        <span>{error || 'حدث خطأ'}</span>
        <button onClick={retry}>إعادة المحاولة</button>
      </div>
    );
  }
  
  return (
    <div className="text-gray-600">
      تم العثور على {resultCount} نتيجة في {latencyMs}ms
    </div>
  );
}

───────────────────────────────────────────────────────────────

5️⃣ أنشئ مكون اختيار المصدر (تطبيق دروس Kare):

📁 dashboard/src/components/search/corpus-selector.tsx

const CORPORA = [
  { id: 'all', label: 'الكل', icon: '🔍' },
  { id: 'fiqh', label: 'الفقه', icon: '📖', count: '47M' },
  { id: 'hadith', label: 'الحديث', icon: '📜', count: '41M' },
  { id: 'kalam', label: 'الكلام', icon: '💭', count: '40M' },
  { id: 'timeline', label: 'التاريخ', icon: '🕐', count: '38M' },
  { id: 'usul', label: 'الأصول', icon: '⚖️', count: '26M' },
];

export function CorpusSelector({ value, onChange }) {
  return (
    <div className="flex gap-2 flex-wrap">
      {CORPORA.map(corpus => (
        <button
          key={corpus.id}
          onClick={() => onChange(corpus.id)}
          className={`px-3 py-1 rounded-full ${
            value === corpus.id ? 'bg-blue-600 text-white' : 'bg-gray-100'
          }`}
        >
          <span>{corpus.icon}</span>
          <span>{corpus.label}</span>
          {corpus.count && <span className="text-xs">({corpus.count})</span>}
        </button>
      ))}
    </div>
  );
}

───────────────────────────────────────────────────────────────

6️⃣ أنشئ مكون اقتراحات البحث (تطبيق دروس Norman + Harris):

📁 dashboard/src/components/search/search-suggestions.tsx

const SUGGESTIONS = [
  'ما حكم صلاة الجماعة؟',
  'أحاديث الصيام في رمضان',
  'الفرق بين المذاهب الأربعة في الزكاة',
  'تاريخ تدوين السنة النبوية',
];

export function SearchSuggestions({ onSelect }) {
  return (
    <div className="mt-2">
      <p className="text-sm text-gray-500 mb-2">جرّب:</p>
      <div className="flex gap-2 flex-wrap">
        {SUGGESTIONS.map(s => (
          <button
            key={s}
            onClick={() => onSelect(s)}
            className="text-sm px-3 py-1 bg-gray-50 rounded-full hover:bg-gray-100"
          >
            {s}
          </button>
        ))}
      </div>
    </div>
  );
}

═══════════════════════════════════════════════════════════════
                    📊 الملخص المطلوب
═══════════════════════════════════════════════════════════════

بعد التنفيذ، أرسل:

✅ الملفات المُنشأة:
   - [ ] backend-client.ts
   - [ ] .env.local
   - [ ] search-status.tsx
   - [ ] corpus-selector.tsx
   - [ ] search-suggestions.tsx

✅ التحديثات:
   - [ ] orchestrator.ts (ربط Backend)

✅ دروس الآباء المطبقة:
   - [ ] حالة التحميل (Friedman)
   - [ ] التغذية الراجعة (Norman)
   - [ ] أيقونات المصادر (Kare)
   - [ ] الاقتراحات (Harris)

✅ GitHub:
   - [ ] تم الرفع

═══════════════════════════════════════════════════════════════

ملاحظة: الـ Backend سيكون جاهزاً على port 8000 قريباً.
طبّق ما تستطيع الآن وسنختبر التكامل معاً.

شكراً! 🚀
```
